{
  "plugin_id": "gen-py-env_setup",
  "plugin_type": "flag-generator",
  "version": "1.0",
  "description": "Converted from generator id 'gen.py.env_setup'.",
  "requires": ["env_name"],
  "produces": [
    {"artifact": "env_dir"}
  ],
  "inputs": {
    "env_name": {"type": "string", "required": true}
  }
}
