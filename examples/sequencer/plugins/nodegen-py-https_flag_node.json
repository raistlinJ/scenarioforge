{
  "plugin_id": "nodegen-py-https_flag_node",
  "plugin_type": "flag-node-generator",
  "version": "1.0",
  "description": "Converted from generator id 'nodegen.py.https_flag_node'.",
  "requires": ["seed", "node_name"],
  "produces": [
    {"artifact": "compose_path"},
    {"artifact": "flag"},
    {"artifact": "https_port"},
    {"artifact": "flag_url_hint"}
  ],
  "inputs": {
    "seed": {"type": "text", "required": true},
    "node_name": {"type": "text", "required": true},
    "flag_prefix": {"type": "text", "required": false},
    "https_port": {"type": "number", "required": false, "default": 8443}
  }
}
