{
  "plugin_id": "gen-py-web_creds",
  "plugin_type": "flag-generator",
  "version": "1.0",
  "description": "Converted from generator id 'gen.py.web_creds'.",
  "requires": ["seed", "secret"],
  "produces": [
    {"artifact": "web_username"},
    {"artifact": "web_password"},
    {"artifact": "artifact_path"}
  ],
  "inputs": {
    "seed": {"type": "string", "required": true},
    "secret": {"type": "string", "required": true},
    "username_prefix": {"type": "string", "required": false}
  }
}
