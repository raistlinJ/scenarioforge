{
  "plugin_id": "gen-py-basic_artifact",
  "plugin_type": "flag-generator",
  "version": "1.0",
  "description": "Converted from generator id 'gen.py.basic_artifact'.",
  "requires": ["seed"],
  "produces": [
    {"artifact": "flag"},
    {"artifact": "artifact_path"}
  ],
  "inputs": {
    "seed": {"type": "string", "required": true},
    "flag_prefix": {"type": "string", "required": false}
  }
}
