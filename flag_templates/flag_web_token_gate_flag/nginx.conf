server {
  listen 80;
  server_name _;

  # Minimal gate: require token query arg
  set $expected_token "tok_6f8b8c2e";

  location / {
    if ($arg_token != $expected_token) { return 403; }

    root /usr/share/nginx/html;
    index index.html;
  }
}
